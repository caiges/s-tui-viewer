# s-tui CSV Viewer

A small static viewer for `s-tui` CSV logs.

## Use

Open `index.html` in a browser, choose an `s-tui` CSV file, and the page will render charts for:

- temperatures
- CPU frequency
- CPU utilization
- fan RPM
- any other numeric columns

The CSV is parsed locally in the browser. Nothing is uploaded.
